# ADR-004: 优化②——静态指令前置与 prompt cache

状态: 已接受 | 日期: 2026-09-14

决策：stage1 user 消息内静态块（任务指令/分层规则/输出要求）前置，
动态块（K 线表/特征/预填）垫后；尾部提醒并入静态区。
开关：`llm_opt.prompt_reorder`（默认开）。

效果：同标的连续轮次请求前缀一致性提升，单轮 prompt cache 命中
65,536 → 125,952 tokens（37% → 73%），输入计费显著下降。
