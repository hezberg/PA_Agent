"""Qt-free service layer shared by any frontend (WebUI now, GUI historically).

Modules here contain the business logic that used to live inside
``pa_agent.gui.main_window`` — analysis submission flow, data-source
switching, settings persistence and demo replay orchestration.
"""
